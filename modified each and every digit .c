//write a program to print each and every digit from the given number
//modified
#include<stdio.h>
int main()
{
    int count=0;
    int i=2;
   for(i=2;i<=10;i++);
   {
       printf("bye buddy");
       {
       printf("ello buddy");
       }
       if(i%2==0)
       {
           count++;
       }
   }
   printf("count=%d",count);
    return 0;
}